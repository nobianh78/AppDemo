<HomeScreen>:
    name: "home"

    Screen:

    MDBottomNavigation:
        panel_color: 0.9, 0.9, 0.9, 1  # Màu nền xám nhạt

        MDBottomNavigationItem:
            name: "home"
            text: "Trang chủ"
            icon: "home"
            theme_text_color: "Custom"  # ✅ Cho phép đổi màu
            on_tab_release: app.change_tab_color(self)  # Gọi hàm đổi màu

        MDBottomNavigationItem:
            name: "profile"
            text: "Cá nhân"
            icon: "account"
            theme_text_color: "Custom"  # ✅ Cho phép đổi màu
            on_tab_release: app.change_tab_color(self)  # Gọi hàm đổi màu

        MDBottomNavigationItem:
            name: "messages"
            text: "Tin nhắn"
            icon: "message"
            theme_text_color: "Custom"  # ✅ Cho phép đổi màu
            on_tab_release: app.change_tab_color(self)  # Gọi hàm đổi màu

        MDBottomNavigationItem:
            name: "friends"
            text: "Bạn bè"
            icon: "account-group"
            theme_text_color: "Custom"  # ✅ Cho phép đổi màu
            on_tab_release: app.change_tab_color(self)  # Gọi hàm đổi màu




